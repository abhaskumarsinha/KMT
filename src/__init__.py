
"""
`src` – Main package for Keypoint-Based Motion Transfer

This package contains all the core components necessary for building, training, and running a keypoint-based motion
transfer system, including models, utilities, and training pipelines.

Subpackages:
------------

- `models`: Core Keras models such as generator, discriminator, and keypoint detectors.
- `utils`: Helper utilities for data loading, geometric transformation, and training setup.

Usage Example:
--------------

To import models and utilities from this package:

```python
from src.models import generator, discriminator, keypoint_detector
from src.utils import dataset_utils, model_utils, training_utils
```

"""
